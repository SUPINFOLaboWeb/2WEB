{% extends '::base.html.twig' %}

{% block title %}{{ post.title }} {{ parent() }}{% endblock %}

{% block body %}
<h1>{{ post.title }}</h1>
<p>{{ post.content }}</p>
<p class="text-right"><em>De {{ post.user.username }}</em></p>
{% endblock %}