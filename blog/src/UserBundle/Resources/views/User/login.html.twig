{% extends '::base.html.twig' %}

{% block title %}Connexion {{ parent() }}{% endblock %}

{% block body %}
<form action="{{ path("user_user_logincheck", {}) }}" method="POST" class="col-sm-6 col-sm-offset-3">
    <h1>Connexion</h1><hr>
    <div class="form-group">
        {% if error %}<p class="text-center text-warning">{{ error.message }}</p>{% endif %}
    </div>
    <div class="form-group">
        <label for="inputUsernameEmail">Nom de compte</label>
        <input type="text" required="required" class="form-control" value="{{ last_username }}" name="_username" id="username" autofocus>
    </div>

    <div class="form-group">
        <label for="inputPassword">Mot de passe</label>
        <input type="password" class="form-control" name="_password" required="required" id="password">
    </div>

    <div class="checkbox pull-left">
        <label>
            <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
            <label for="remember_me">Rester connecté</label>
        </label>
    </div>

    <button type="submit" class="pull-right btn btn-success">Connexion</button>
</form>
{% endblock %}