{% extends '::base.html.twig' %}

{% block title %}Inscription {{ parent() }}{% endblock %}

{% block body %}
<div class="col-sm-6 col-sm-offset-3">
    <h1>Inscription</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_rest(form) }}
    {{ form_end(form) }}
</div>
{% endblock %}